{
  "name": "billqr",
  "version": "1.0.0",
  "description": "QR-based product scanning, cart management, and billing system",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "setup:db": "cd server && npm run schema && npm run seed",
    "install:all": "cd server && npm install && cd ../client && npm install"
  },
  "private": true
}
